import 'package:flutter/material.dart';

class LocalShopRegistryScreen extends StatelessWidget {
  const LocalShopRegistryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      appBar: AppBar(
        title: Text('Local Shop Registry'),
      ),
      body: Center(
        child: Text('Local Shop Registry Screen'),
      ),
    );
  }
}
