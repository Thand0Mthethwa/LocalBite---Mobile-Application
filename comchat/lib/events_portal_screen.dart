import 'package:flutter/material.dart';

class EventsPortalScreen extends StatelessWidget {
  const EventsPortalScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      appBar: AppBar(
        title: Text('Events Portal'),
      ),
      body: Center(
        child: Text('Events Portal Screen'),
      ),
    );
  }
}
